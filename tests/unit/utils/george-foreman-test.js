import GeorgeForeman from '../../../utils/george-foreman';
import { module, test } from 'qunit';

module('Unit | Utility | george foreman');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = new GeorgeForeman();
  assert.ok(result);
});
